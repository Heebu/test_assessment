// // String formatted = DateFormat('dd MMM yyyy, h:mm a').format(date);
// //
// //
// {String month, } timestampConvert(DateTime date){
//
// }

bool isSameDay(DateTime a, DateTime b) {
  return a.year == b.year && a.month == b.month && a.day == b.day;
}
